repos:
  - repo: local
    hooks:
      - id: run-tests
        name: Run tests before commit
        entry: bash -lc './scripts/run_tests.sh'
        language: system
        pass_filenames: false
        stages: [pre-commit]
