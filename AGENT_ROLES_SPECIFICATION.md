# Agent角色规范详细定义

## 🎯 13个Agent + 1人工位详细职责 (来自之前分析)

### 📊 数据平面Agent (6个)

#### 1. Ingest 数据采集官
```
目标: 完整、准时、幂等入库
输入: 外部盘口/开奖结果
输出: 标准化raw事件 (带period、event_time)
SLO:  event→ingest ≤1s(P99.9)，重复率<0.1%
红线: 未签名、时间漂移包
TDR:  触发→拒收并打clock_drift/raw_poisoned标签→只记录→告警
禁令: 补写虚构时间、私改period
```

#### 2. Normalize 对齐官
```
目标: 口径统一、去重、异常填补不越界
输出: normalized事件+窗口标签
SLO:  ≤500ms；异常率<0.5%
红线: 导致窗口错位/样本污染的修补
TDR:  触发→冻结Normalize→仅透传raw→人工复核后解冻
禁令: 跨日拼接以"提高样本"
```

#### 3. Feature 特征官
```
目标: 近因窗（12-16，半衰5-6）特征可复现
输出: 特征快照+窗口元数据
SLO:  ≤500ms；口径变更需变更单
红线: 历史增强、跨窗泄漏
TDR:  触发→回滚上版口径→Learner暂停上新24h
禁令: 隐式改变窗口/半衰
```

#### 4. Score 量化评分官
```
目标: 产pred_prob、持续校准（Brier/SCE）
SLO:  ≤400ms；校准漂移<阈
红线: LLM干预分数；模型漂移未过评测
TDR:  触发→切上一个稳定模型→记录drift事件→观察窗内禁止切回
禁令: 在事故期"救火调参"
```

#### 5. Risk & Rules 风控裁决官
```
目标: 只押EV>0；分层Kelly；去相关与CAP护栏
固定: threshold_conf_raw=0.78、kelly=0.25、bet_cap=0.02
红线: 无审批改阈值/CAP
TDR:  触发→降级CL1轻仓或只记录→报Approver
禁令: 与Exec协商"先下再说"
```

#### 6. Exec 执行官
```
目标: 下单幂等且可回放；3次失败→降级"只记录不下单"
红线: 对账缺Ack；新鲜度红线
TDR:  触发→切只记录→Reconciler追偿→Approver决定补单
禁令: 绕过新鲜度红线强行下单
拒绝: 在数据新鲜度告警期间下单
```

### 🛡️ 保证平面Agent (3个)

#### 7. Reconciler 对账官
```
目标: 事后真相；对齐期号、成交、PnL；产出"偏差单"
触发: 每期结果入库≤2s；T+0出日报
SLO:  T+0当日出清；差异<阈
红线: 连续5次无确认或偏差>1σ
TDR:  触发→标红period→Exec只记录→出偏差清单
禁令: 用预测覆盖对账空洞
```

#### 8. Observer 守望官
```
目标: 四指标看门（覆盖/胜率/敞口/触发率）
动作: 任一越阈→Flow Conductor自动降级/冻结 + Chat告警
SLO:  告警命中率≥0.9，噪声率≤0.1
红线: Freshness/Clock drift/Watermark
TDR:  触发→发红卡→Flow Conductor执行剧本
禁令: 静默失败
```

#### 9. Flow Conductor 编排/发布官
```
目标: 心跳→灰度→回滚三件事做快做对
流控: 按"期号mod N"灰度，指标劣化即回滚；冻结规则仅Approver可解
SLO:  红卡→处置≤1分钟；回滚≤30秒
红线: 灰度泄漏
TDR:  触发→回滚→冻结Learner 24h
禁令: 人工改流量绕过取模
```

### 🧠 学习治理平面Agent (4个)

#### 10. Learner 研究官
```
目标: 只在近因样本上发现可验证组合；管好CL3/2/1阈值与族内外上限
拒绝: 多重检验未过（FDR）/样本量不足的"好看故事"
红线: 样本不足/多检验未过
TDR:  触发→候选退回草案→仅保留已证实组合
禁令: 把后验故事当证据
```

#### 11. Model Router 航管塔
```
目标: 把问题交给对的模型，时延/成本/风险三角最优
路由建议:
• 快速问诊 & 低风险 → gemini-2.5-flash
• 复杂解释 & 高风险 → gpt-5 ↔ claude-opus双模
• 数理与约束自检 → deepseek-r1
• 超长中文资料 → qwen3-235b-a22b
• 多模态报表/截图 → gemini-2.5-pro
红线: 输出决定性"改信号/改阈值"的建议
TDR:  触发→降级单模/短输出→成本闸门
禁令: 生成"新信号"
```

#### 12. Arbiter 仲裁官
```
目标: 双模互挑刺，只落"库内可复现的要点"；给一致性分
红线: 要点不可在BigQuery重现
TDR:  触发→标红解释→禁止变更生效
禁令: 采纳"漂亮但不可复现"的理由
```

#### 13. Governor 治理官
```
目标: 预算/配额/密钥/合规；成本熔断
红线: 月度消耗≥90%仍跑慢车道
TDR:  触发→改快车道→暂停Learner上新
禁令: 在事故期开放外呼到未知域
```

### 👑 人工审批位

#### 14. Approver (人)
```
目标: 唯一能批Kelly/阈值/CAP与上新/回滚/解冻
红线: 缺trace_id或证据不可复现的提案
TDR:  退回、要求补证、或签字生效
禁令: 口头批准
权限: 唯一能改Kelly/阈值/CAP、上新/回滚、解冻
```

## 🎯 模型分工详细定义

### 🤖 生产预测模型 (Vertex AI)
```
职责: 实际预测和交易信号生成
模型: 10个专业Vertex AI模型
输出: p_cloud/p_map/p_size → p_star_ens
频率: 每期实时预测 (3.5分钟)
质量: 51.49%准确率，边际盈利水平
```

### 🧠 解释分析模型 (AI/ML API)
```
职责: 解释、分析、监控、诊断
模型: 6个顶级AI模型
输出: 解释报告、分析建议、监控告警
频率: 按需调用，智能路由
质量: 基于BigQuery事实的可复现解释
```

## 🚨 当前接通状态

### ❌ 问题发现
```
1. Vertex AI模型: 存在但实时预测中断9小时
2. AI/ML API模型: AIML_API_KEY未设置，完全未接通
3. 三源数据: 存在但可能不完整或更新中断
4. 集成机制: p_ensemble_today_norm_v数据停滞
```

### 🔧 需要修复
```
1. 恢复Vertex AI实时预测流程
2. 设置AI/ML API密钥接通高级模型
3. 修复三源数据更新机制
4. 恢复集成投票正常工作
```

**我记起来了！这些Agent角色定义来自之前的深度分析！** 📋

**当前问题是模型虽然高级但接通状态有问题！** 🚨

cursor
